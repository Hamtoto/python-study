# 🚀 Clean DevContainer Requirements (venv 환경용)
# dual_face_tracker_plan.md 기준 필수 패키지만 포함

# =============================================================================
# 🎯 Dockerfile에서 별도 설치하는 패키지들 (여기서 제외)
# =============================================================================
# av==11.0.0               # 소스 빌드로 NVENC/NVDEC 지원
# tensorrt-cu12==10.5.0    # CUDA 12.8 호환 버전
# PyNvVideoCodec           # NVIDIA 공식 비디오 코덱
# torch, torchvision, torchaudio  # nightly cu128 버전

# =============================================================================
# 🔥 PyTorch 생태계 (RTX 5090 최적화)
# =============================================================================
# 주의: torch, torchvision, torchaudio는 Dockerfile에서 nightly cu128로 설치됨
# torch==2.7.1+cu128  # --index-url https://download.pytorch.org/whl/nightly/cu128
# torchvision==0.18.1+cu128
# torchaudio==2.7.1+cu128

# =============================================================================
# 🧮 수치 계산 및 과학 컴퓨팅
# =============================================================================
numpy==1.24.3
scipy==1.10.1
scikit-learn==1.3.0

# GPU 가속 연산 (CUDA 커널 직접 구현용)
cupy-cuda12x>=12.0.0

# =============================================================================
# 🎬 비디오/이미지 처리 (경량화)
# =============================================================================
# OpenCV는 CUDA DEB 패키지에서 제공 (opencv-python 설치 안함)

# FFmpeg Python 바인딩 (필요시에만)
ffmpeg-python==0.2.0

# 이미지 처리
pillow>=7.1.2

# =============================================================================
# 🤖 AI/ML 라이브러리 (의존성 충돌 방지)
# =============================================================================
# ultralytics, facenet-pytorch는 Dockerfile에서 --no-deps로 별도 설치

# ultralytics 의존성들을 직접 설치 (opencv-python 제외)
matplotlib>=3.3.0
pandas>=1.1.4
psutil>=5.8.0
py-cpuinfo>=2.0.0
pyyaml>=5.4.1
requests>=2.25.1
seaborn>=0.11.0
thop>=0.1.1

# =============================================================================
# 🛠️ 유틸리티 및 로깅
# =============================================================================
tqdm==4.66.1
PyYAML==6.0.1
loguru==0.7.2
typer==0.9.0
rich==13.5.2

# =============================================================================
# 📊 모니터링 및 성능 측정
# =============================================================================
psutil==5.9.5
pynvml==11.5.0

# 선택적: 실험 추적 (필요시에만 활성화)
# wandb==0.15.10

# =============================================================================
# 🧪 개발/테스트 도구
# =============================================================================
pytest==7.4.2
pytest-asyncio==0.21.1
black==23.7.0
isort==5.12.0
mypy==1.5.1

# =============================================================================
# ❌ 제거된 문제 패키지들 (PEP 668 충돌 방지)
# =============================================================================
# moviepy==1.0.3           # → av==12.3.0으로 대체 (PyAV NVDEC)
# pathtools               # → setuptools 호환성 이슈로 제거